<UserControl x:Class="MVVMNavigation.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMNavigation.Views"
             xmlns:components="clr-namespace:MVVMNavigation.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <components:NavigationBar Grid.Row="0" DataContext="{Binding NavigationBarViewModel}"/>

        <Grid Grid.Row="1" MaxWidth="1000" Margin="20">

            <StackPanel>
                <TextBlock FontSize="32"
                   Text="Home" />

                <TextBlock Margin="0 8 0 0"
       Text="{Binding WelcomeMessage}" />

                <Button Margin="0,20,0,0" Padding="12 4"
    HorizontalAlignment="Left"
    Content="Start"
    Command="{Binding NavigateloginCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
